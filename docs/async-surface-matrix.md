# AdvisorBook async surface matrix

This matrix tracks browser-visible AdvisorBook routes that issue async
resource requests. It is the handoff surface for loading, error, retry,
desktop evidence, and mobile evidence coverage.

## Scope

- Source routes were inspected from `src/web/*.ts`.
- Legacy checked-in directory pages were inspected from `harper-app/web/*.html`.
- Shared navbar/session/search behavior applies anywhere a page mounts one of
  the design-system page templates with `refreshMe` and `search`.
- Generated deploy JavaScript under `harper-app/web/**/*.js` is ignored by git
  and is not the source of record; regenerate it with `bun run build`.

## Route matrix

| Surface | Source file | Request path | Loading pattern | Error pattern | Retry / refresh action | Desktop Playwright evidence | Mobile Playwright evidence | Follow-up classification |
|---|---|---|---|---|---|---|---|---|
| Home feed `/` and `/index.html` | `src/web/index.ts` | `GET /Feed` | Two `SkeletonCard()` placeholders render in the center rail until the feed resolves. | Center rail clears and shows `EmptyCard` titled "Could not load feed" with the REST error message. | Browser refresh; no in-page retry action. | `RUN_WEB_ASYNC_STATES=1 bunx vitest run tests/web_async_states.test.ts` asserts skeleton, empty, and error states and emits `async-desktop-feed-loading.png`, `async-desktop-feed-empty.png`, and `async-desktop-feed-error.png`. `bun run smoke` asserts the live feed content. | `RUN_WEB_ASYNC_STATES=1 bunx vitest run tests/web_async_states.test.ts` emits matching `async-mobile-*` feed screenshots. `bun run smoke` asserts the 320px mobile feed/nav journey. | In scope for route-level loading/error coverage. |
| Compliance `/regulatory` (`regulatory.html` still works) | `harper-app/web/regulatory.html` | `GET /Feed` | No route-specific skeleton; page chrome/nav render while the feed request is pending. | Center rail shows `EmptyCard` titled "Could not load compliance events" with the REST error message. | Browser refresh; no in-page retry action. | `bun run smoke` asserts the deployed clean route, legacy route compatibility, compliance card, disclosure event rendering, regulatory context, and absence of the compliance load-error card; screenshot `06-compliance.png`. | `bun run smoke` breakpoint coverage verifies the shared page chrome at mobile widths; compliance content uses the same stacked card layout as the feed/error evidence. | In scope; should eventually use the same source build path as other TS pages. |
| Article detail `/articles/<slug-id>` and `/article.html?id=<id>` | `src/web/article.ts` | `GET /ArticleView/<id>` | Page chrome renders; detail column stays empty until the article request resolves. | Center rail shows `EmptyCard` titled "Error" for failed requests; successful not-found payloads show "Article not found". | Browser refresh or open another article; no in-page retry action. | `bun run smoke` finds a `/Feed` article with provenance, opens the canonical article route, and asserts extracted-facts rows; screenshot `05-article-detail.png`. | `bun run smoke` breakpoint coverage verifies detail-page shared chrome and overflow behavior through the same responsive template. | In scope for route-level loading/error coverage. |
| Advisor profile `/advisors/<slug-id>` and `/advisor.html?id=<id>` | `src/web/advisor.ts` | `GET /AdvisorProfile/<id>` | Page chrome renders; profile columns stay empty until the advisor request resolves. | Center rail shows `EmptyCard` titled "Error" for failed requests; missing id shows "No advisor selected"; successful not-found payloads show "Advisor not found". | Browser refresh or open another advisor; no in-page retry action. | `bun run smoke` opens a real advisor from the feed flow and asserts header, timeline, disclosures, sanctions, FINRA CRD badge, and BrokerCheck attribution. | `bun run smoke` breakpoint coverage verifies profile template chrome and no horizontal overflow down to 320px. | In scope for route-level loading/error coverage. |
| Firm profile `/firms/<slug-id>` and `/firm.html?id=<id>` | `src/web/firm.ts` | `GET /FirmProfile/<id>` plus paginated `GET /FirmAdvisors/<id>?status=current&limit=50[&cursor=...]` and `GET /FirmAdvisors/<id>?status=past&limit=50[&cursor=...]` when roster sections mount. | Page chrome renders while profile request is pending; roster sections use `Paginated` status text and load-more controls while page requests are pending. | Profile request failure shows `EmptyCard` titled "Error"; paginated roster failure shows inline `Couldn't load more: ...` status. | Browser refresh for profile; `Load more` button retries/continues roster pagination when pagination is not done. | `bun run smoke` opens a real firm profile, asserts current/past advisors, inbound transitions, branch rail content, and roster pagination behavior. | `bun run smoke` breakpoint coverage verifies profile template chrome and no horizontal overflow down to 320px. | In scope for route-level and nested pagination coverage. |
| Team profile `/teams/<slug-id>` and `/team.html?id=<id>` | `src/web/team.ts` | `GET /TeamProfile/<id>` | Page chrome renders; profile columns stay empty until the team request resolves. | Center rail shows `EmptyCard` titled "Error" for failed requests; missing id shows "No team selected"; successful not-found payloads show "Team not found". | Browser refresh or open another team; no in-page retry action. | `bun run smoke` opens a real team profile and asserts the header, current members, and metric snapshot rows. | `bun run smoke` breakpoint coverage verifies profile template chrome and no horizontal overflow down to 320px. | In scope for route-level loading/error coverage. |
| Advisor directory `/advisors.html` | `harper-app/web/advisors.html` | `GET /PublicAdvisors?limit=50[&cursor=...]` | `Paginated` renders polite status text and a load-more control while cursor pages are pending; right-rail total starts as "Loading...". | `Paginated` shows inline `Couldn't load more: ...` status. | `Load more` button retries/continues pagination when pagination is not done. | `bun run smoke` asserts first-page advisor rows, total count, pre-click count, load-more append behavior, and duplicate prevention. | `bun run smoke` includes 320px mobile drawer/search checks after visiting the directory flow. | In scope; checked-in HTML is currently the source of this route. |
| Firm directory `/firms.html` | `harper-app/web/firms.html` | `GET /PublicFirms?limit=50[&cursor=...]` | `Paginated` renders polite status text and a load-more control while cursor pages are pending; right-rail total starts as "Loading...". | `Paginated` shows inline `Couldn't load more: ...` status. | `Load more` button retries/continues pagination when pagination is not done. | `bun run smoke` asserts bounded first-page firm rows, total count, load-more append behavior, and duplicate prevention; screenshots `06-firms.png` and `06-firms-pagination.png`. | `bun run smoke` breakpoint coverage verifies the shared directory layout at mobile widths. | In scope; checked-in HTML is currently the source of this route. |
| Team directory `/teams.html` | `harper-app/web/teams.html` | `GET /PublicTeams?limit=50[&cursor=...]` | `Paginated` renders polite status text and a load-more control while cursor pages are pending; right-rail total starts as "Loading...". | `Paginated` shows inline `Couldn't load more: ...` status. | `Load more` button retries/continues pagination when pagination is not done. | `bun run smoke` asserts bounded first-page team rows, total count, load-more append behavior, and duplicate prevention; screenshots `06-teams.png` and `06-teams-pagination.png`. | `bun run smoke` breakpoint coverage verifies the shared directory layout at mobile widths. | In scope; checked-in HTML is currently the source of this route. |
| Sign-in `/login.html` | `src/web/login.ts` | `POST /Login`, then `GET /Me` through `refreshMe()` after successful login. | Submit button disables and changes to "Signing in..." during the login request. Navbar session state also loads through shared `/Me`. | Inline form error shows auth-safe copy for 401/403 or the REST error message for other failures. | Submit the form again after editing credentials; browser refresh reloads shared session state. | `RUN_WEB_ASYNC_STATES=1 bunx vitest run tests/web_async_states.test.ts` asserts failed-login safe recovery copy and guards against leaking raw authorization-policy text. `bun run smoke` records the deployed anonymous sign-in affordance. | `bun run smoke` verifies mobile sign-in affordance in the drawer at 320px. | In scope for auth async coverage; mocked `/Login` covers failure copy until test credentials exist. |
| Shared session affordance on templated pages | `src/web/app.ts`, `src/web/design-system/organisms-nav.ts`, `src/web/design-system/templates.ts` | `GET /Me` | Navbar renders immediately; the auth slot updates when `refreshMe()` resolves. | Auth failures render a safe sign-in/public-browse fallback instead of blocking the page. | Browser refresh; sign out uses `POST /Logout` and reloads or returns to `/`. | `RUN_WEB_ASYNC_STATES=1 bunx vitest run tests/web_async_states.test.ts` asserts `/Me` failure guidance while public feed content remains visible. | `RUN_WEB_ASYNC_STATES=1 bunx vitest run tests/web_async_states.test.ts` captures mobile feed evidence with the shared nav loaded; `bun run smoke` verifies mobile drawer/session affordance. | Shared in-scope surface; tested through the shared template and mobile nav layout. |
| Shared global search on templated pages | `src/web/app.ts`, `src/web/design-system/organisms-search.ts`, `src/web/design-system/organisms-nav.ts` | `GET /Search?q=<query>` for queries with at least two characters. | Dropdown shows `Searching for "<query>"...` after debounce; stale slower responses are ignored. | Dropdown shows `Search failed: <message>` for request failure or timeout. | Type again to issue a new search; Escape/click outside closes the dropdown. | `bun run smoke` asserts dropdown open, selectable suggestions, supported result rendering, keyboard selection, and clean profile-route navigation. | `bun run smoke` verifies search remains readable at 320px and mobile nav has no horizontal overflow. | Shared in-scope surface; tested once per desktop/mobile nav layout. |

## Hidden and legacy surface classification

| Surface | Classification | Rationale |
|---|---|---|
| Legacy query-string profile URLs (`/advisor.html?id=...`, `/firm.html?id=...`, `/team.html?id=...`, `/article.html?id=...`) | Duplicate coverage required with canonical routes. | `src/web/urls.ts` intentionally supports legacy query IDs and canonicalizes successful renders to clean slug routes. Use the same profile assertions with one query-string smoke per entity kind. |
| Generated deploy JavaScript in `harper-app/web/**/*.js` | Not a separate coverage target. | The generated files are build output from `src/web/` and are ignored by git. Coverage should target source behavior, then run `bun run build` to prove generated assets are current. |
| Checked-in legacy directory HTML (`advisors.html`, `firms.html`, `teams.html`, `regulatory.html`) | Follow-up migration candidate, but currently in scope. | These pages are browser-reachable and issue public resource requests, so they need coverage now. A future migration can move them under `src/web/` and keep the same matrix rows. |
| Static-only pages without resource calls | Out of scope for async matrix. | `harper-app/web/*.html` pages that do not call `api()`, `postJson()`, `refreshMe()`, or `search()` have no route-specific async surface beyond shared template behavior. |
