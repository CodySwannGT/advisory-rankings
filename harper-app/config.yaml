# Harper component config — declares this directory as a Harper application.
# Docs: https://docs.harperdb.io/docs/developers/components

# Auto-export tables defined in schema.graphql at REST routes.
graphqlSchema:
  files: '*.graphql'

# HTTP REST interface (port 9926 by default) for every @export-ed table
# and for the custom resources below.
rest: true

# Custom JS resources (Feed, ArticleView, FirmProfile, AdvisorProfile,
# TeamProfile) backing the web UI.  See resources.js.
jsResource:
  files: 'resources.js'

# Clean public routes for SEO-friendly directory and profile URLs.
# These serve the same static HTML shells as the legacy *.html paths.
fastifyRoutes:
  urlPath: '.'
  files:
    - 'branches/index.js'
    - 'firms/index.js'
    - 'advisors/index.js'
    - 'teams/index.js'
    - 'articles/index.js'
    - 'data-coverage/index.js'
    - 'investor-proof/index.js'
    - 'recruiting/index.js'
    - 'recruiting-deal-gaps/index.js'
    - 'recruiting-shortlist/index.js'
    - 'research-freshness/index.js'
    - 'source-triage-route/index.js'
    - 'rankings/index.js'
    - 'regulatory/index.js'
    - 'regulatory-discrepancies/index.js'
    - 'corrections/index.js'
    - 'compare/index.js'
    - 'report-packet/index.js'
    - 'watchlists/index.js'
    - 'login/index.js'
    - 'favicon/index.js'
    - 'static-web/index.js'

# AdvisorBook web UI.  Served at the same origin as REST so the
# browser can `fetch('/Feed')` without CORS.  GET / serves index.html.
# `**/*` is recursive so the design-system/ subdirectory ships too.
static:
  files: 'web/**'
  extensions:
    - 'html'
